server:
  port: ${SERVER_PORT:8080}
spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: transaction
  datasource:
    url: ${JDBC_URL:jdbc:mysql://localhost:3306/account_test}
    username: ${JDBC_USER:root}
    password: ${JDBC_PASSWORD:Mit@varun236}
    driver-class-name: com.mysql.cj.jdbc.Driver
